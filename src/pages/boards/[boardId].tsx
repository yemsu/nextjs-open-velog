import { useRouter } from "next/router"
import styled from "styled-components"

interface BoardsProps {

}

function Boards(props: BoardsProps) {
  const route = useRouter()

  
  return (
    <div>Boards</div>
  )
}

export default Boards